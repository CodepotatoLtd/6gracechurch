"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.globAssets = void 0;
const bud_support_1 = require("@roots/bud-support");
async function globAssets(search) {
    try {
        this.log(`search`, search);
        const results = await bud_support_1.globby.globby(search, { cwd: this.path('@src') });
        this.log(`results`, results);
        if (!results.length) {
            this.error(`bud.entry found no files matching ${JSON.stringify(search)}. check your config for errors. files should be specified relative to ${this.path('@src')}. fast glob syntax can be referenced here https://git.io/JkGbw`);
        }
        return results;
    }
    catch (error) {
        throw new Error(error);
    }
}
exports.globAssets = globAssets;
