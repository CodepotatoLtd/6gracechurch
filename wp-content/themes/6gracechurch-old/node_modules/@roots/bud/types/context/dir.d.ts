export declare class Dir {
    cwd?: string;
    project: string;
    constructor(cwd?: string);
    set(dir: 'cwd' | 'project', path: string): this;
    check(path: string): Promise<string | false>;
    setProject(path: string): Promise<this>;
    find(): Promise<Dir>;
}
//# sourceMappingURL=dir.d.ts.map